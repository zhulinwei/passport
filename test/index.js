// const sinon = require('sinon');
// const mongo = require('../src/database').mongo;
// console.log('mongo123:', mongo);
// sinon.stub(mongo, 'getDb').returns({ collection: () => {} });

// module.exports = {
//  controller: require('./controller')
// };
